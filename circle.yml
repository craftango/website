machine:
  node:
      version: 0.11.8

dependencies:
  override:
   - npm install
deployment:
   production:
     branch: master
     commands:
      - scp * craftango@domainak.com:/home/craftango/www/
